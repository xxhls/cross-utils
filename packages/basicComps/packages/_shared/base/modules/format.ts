import { formatUtils } from '../../dataUtils';
export { formatUtils };

// 导出常用方法
export const { 
  format: formatPrice 
} = formatUtils.price;

export const { 
  format: formatTime 
} = formatUtils.time; 