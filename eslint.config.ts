import unjs from "eslint-config-unjs";

export default unjs({
  ignores: [
    // ignore paths
    "apps",
  ],
  rules: {
    // rule overrides
  },
  markdown: {
    rules: {
      // markdown rule overrides
    },
  },
});
